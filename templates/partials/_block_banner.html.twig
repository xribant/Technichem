 <section class="wrapper {% if page.banner %}bg-image text-white" data-image-src="assets/media/{{ page.banner }}{% else %}bg-gradient-reverse-primary text-white{% endif %}" style="height: 11rem;">
    <div class="container pt-10 pb-19 pt-md-14 pb-md-20 text-center">
        <div class="row">
            <div class="col-sm-10 col-md-12 col-lg-12 col-xl-12 col-xxl-12 mx-auto">
            <h1 class="display-1 mb-3">{{ page.title }}</h1>
            <nav class="d-inline-block" aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="{{ path('app_homepage') }}">Accueil</a></li>
                    <li class="breadcrumb-item active" aria-current="page">{{ page.title }}</li>
                </ol>
            </nav>
            <!-- /nav -->
            </div>
            <!-- /column -->
        </div>
        <!-- /.row -->
    </div>
    <!-- /.container -->
</section>